clc;
clear all;
close all;
format short g;
% -------------------------------------------------- %
fileList = dir('figures/');
mkdir('filteredFigures')
for iF = 3:length(fileList)
    if fileList(iF
end